import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.helix.hardware',
  appName: 'Helix General Hardware',
  webDir: 'dist'
};

export default config;
