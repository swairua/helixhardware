# ==========================================
# DATABASE CONFIGURATION - EXTERNAL API ONLY
# ==========================================
# This application ONLY uses the custom API endpoint (helixgeneralhardware.com/api.php)
# Supabase is NOT used in any configuration
# Database provider is set to 'external-api' by default in src/integrations/database/manager.ts

# VITE_DATABASE_PROVIDER=external-api (default, do NOT override)

# External API Configuration
# Default in dev: Auto-detects localhost or uses helixgeneralhardware.com/api.php
# Default in prod: Uses relative /api.php (works on any domain)
VITE_EXTERNAL_API_URL=https://helixgeneralhardware.com/api.php

# ==========================================
# OPTIONAL: For local development
# ==========================================
# Use local auth server instead of external API
# VITE_USE_LOCAL_AUTH=true

# API Auth Token for admin operations (if needed)
# API_AUTH_TOKEN=your_admin_token_here
